version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/deploy

hooks:
  ApplicationStop:
    - location: scripts/stop.sh
      timeout: 60
      runas: root

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: root